# Wedding
This project is to build a wedding website with claude code.


whenever you make changes to the site going forward, you just run these three commands in the VSCode terminal to push updates live:
git add .
git commit -m "describe what you changed"
git push origin main

GitHub Pages will automatically republish within about 60 seconds. That's your deploy workflow from here on out.